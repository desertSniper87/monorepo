package chapter8SocketsForClients;

import java.net.*;
import java.io.*;
import javax.swing.*;

public class Example81dayTimeClient {
	public static void main (String[] args){
		String hostname = args > 0 ? args[0] : "time.nist.gov";
		Socket socket = null;
	}
	
	public class dayTimeGUI extends JFrame{
		panel = new JPanel();
		button = new JButton();
		label = new JLabel();
		
		panel.add (button);
		panel.add (label);
		this.add(panel);
	}

}
